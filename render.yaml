services:
  - type: web
    name: gestor-tareas
    env: java
    buildCommand: ./mvnw clean package
    startCommand: java -jar target/*.jar
    envVars:
      - key: PORT
        value: 8080
    plan: free
    autoDeploy: true
